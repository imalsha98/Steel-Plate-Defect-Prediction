1). open this using Vs code
2). then open vs code terminal and go to the CI directory using 'cd CI' command
3). change model path in app.py file
4). After that run this command "streamlit run app.py"
